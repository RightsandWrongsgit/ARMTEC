# Site Settings
logo: /book/images/media/ARMTEC Logo Medium+.png
title: [Move Forward with...]
description: [This is Our Technical Support Division]
baseurl: /ARMTEC

# Build Settings
markdown: kramdown
remote_theme: minimal
# remote_theme: RightsandWrongsgit/minimal
plugins:
  - jekyll-target-blank

# Enable safe mode
safe: true

# Whitelist plugins under safe mode.
# Note that if `some-other-jekyll-plugin` is not listed here it will not be loaded under safe mode.
whitelist:
  - jekyll-target-blank
